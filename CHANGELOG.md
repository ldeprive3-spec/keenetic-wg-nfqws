# Changelog

Все значительные изменения проекта задокументированы в этом файле.

---

## [2.3] - 2026-01-21

### Added

- ✅ **Полностью автоматический установщик** `install.sh`:
  - проверка всех требуемых компонентов Keenetic (IPv6, Netfilter, WireGuard, Entware);
  - автоматическая установка `nfqws-keenetic` через `opkg` с нужными зависимостями;
  - создание конфига `/opt/etc/nfqws/nfqws.conf` с балансным профилем обфускации (fake + split2, repeats=4);
  - включение автозапуска и немедленный старт NFQWS.

- ✅ **Менеджер конфигурации** `nfqws-config.sh`:
  - три преднастроенных профиля обфускации: `weak` (минимум), `medium` (рекомендуется), `aggressive` (максимум);
  - интерактивное меню и командная строка;
  - просмотр статуса NFQWS и WireGuard-интерфейсов;
  - просмотр логов.

- ✅ **Полная документация** в одном файле `README.md`:
  - быстрый старт (одна команда);
  - требования и установка компонентов;
  - получение WARP-конфига;
  - включение WireGuard;
  - управление и проверка;
  - troubleshooting.

### Changed

- 📝 **Переезд на минималистичную модель**:
  - убрана вся ненужная функциональность (DNS-серверы, hosts-файлы, автообновления);
  - фокус только на **WireGuard + NFQWS** для DPI-обхода handshake;
  - структура проекта упрощена (нет примеров, docs, tests).

- 🎯 **Нулевая сложность для пользователя**:
  - исключены ситуации, когда нужно что-либо раскомментировать или руками дописывать параметры;
  - всё задано по умолчанию (средний профиль обфускации — оптимум для большинства);
  - пользователь может только менять профиль если сайт блокируется.

- 🔄 **Переработана логика NFQWS**:
  - конфиг создаётся автоматически с рекомендуемыми параметрами;
  - сервис включается и запускается сразу;
  - логирование отключено по умолчанию (только при необходимости диагностики).

### Fixed

- 🐛 Убрана путаница с AmneziaWG (это параметры WireGuard, а не отдельное приложение).
- 🐛 Уточнена разница между WARP-конфигами для WireGuard и AWG 1.5 для AmneziaVPN.
- 🐛 Убраны неправильные предположения про раскомментирование параметров в конфигах.

### Security

- 🔒 Конфиги сохраняются с правильными правами доступа (644 для конфига, 755 для скриптов).
- 🔒 Все проверки на root/admin перед запуском.

---

## [2.2] - 2026-01-20

### Changed

- 📁 Создание отдельного репозитория `keenetic-wg-nfqws` (ранее был часть большего проекта).
- 🗑️ Удалены каталоги `examples/`, `docs/`, `tests/` (не нужны для минимального проекта).
- 🗑️ Удалены все скрипты, связанные с dnsmasq, hosts-файлами и DNS-обновлениями.

---

## [1.0] - 2026-01-19

### Initial

- 🚀 Базовый прототип проекта.
- ✅ Базовая установка NFQWS на Keenetic через Entware.
- ✅ Ручное редактирование конфигурации.
- ✅ Рестарт сервиса через команды.
