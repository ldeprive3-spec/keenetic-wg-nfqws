# Keenetic AmneziaWG + NFQWS DPI Bypass

Автоматизированная установка **AmneziaWG** (Cloudflare WARP) + **NFQWS** на маршрутизаторе **Keenetic** для полного обхода DPI блокировок провайдера с использованием обфускации пакетов.

🇷🇺 **Русская документация** | 🛡️ **DPI Protection** | 🚀 **Полная автоматизация**

---

## ⚡ Быстрый старт

```bash
# На маршрутизаторе Keenetic через SSH или терминал:
curl -fsSL https://raw.githubusercontent.com/ldeprive3-spec/keenetic-wg-nfqws/main/install.sh | sh
```

---

## 📋 Требования

- **Маршрутизатор:** Keenetic 5.0.4+
- **Компоненты:** IPv6, Netfilter, WireGuard VPN, Entware
- **Память:** 512 MB свободно
- **USB:** Флешка 1GB+ для Entware

---

## ⚙️ Установка компонентов (ОБЯЗАТЕЛЬНО!)

Перед запуском скрипта установите компоненты Keenetic:

### 1. Веб-интерфейс

Откройте: **http://192.168.1.1** → **Управление** → **Параметры системы** → **Изменить набор компонентов**

### 2. Отметьте галочки ☑

```
☑ Протокол IPv6                    (КРИТИЧНО для NFQWS)
☑ Модули ядра подсистемы Netfilter (появляется после IPv6)
☑ WireGuard VPN                    (ОБЯЗАТЕЛЬНО для AmneziaWG)
☑ Entware                          (для установки NFQWS)
☑ SSH сервер                       (для удаленного доступа)
```

### 3. Нажмите "Обновить KeeneticOS" и дождитесь перезагрузки (5-10 минут)

### 4. Проверка установки

```bash
ssh admin@192.168.1.1

# Проверка IPv6
ip -6 addr show

# Проверка Netfilter
lsmod | grep nf_queue

# Проверка WireGuard
show interface | grep wireguard

# Проверка Entware
opkg --version
```

---

## 🚀 Получение конфига AmneziaWG

### Способ 1: Online Generator (самый простой)

1. Откройте: https://warpgen.net
2. Выберите **AmneziaWG**
3. Нажмите **Generate**
4. Скопируйте конфиг, сохраните как **warp.conf**

### Способ 2: Через bash-warp-generator

```bash
# На удаленном сервере (если 1-й не работает):
bash <(wget --inet4-only -qO- https://raw.githubusercontent.com/ImMALWARE/bash-warp-generator/main/warp_generator.sh)
```

---

## 🔧 Конфигурация AmneziaWG

### 1. Загрузка конфига в Keenetic

**Веб-интерфейс:**
1. **Интернет** → **Другие подключения** → **WireGuard**
2. **Добавить подключение** → загрузите **warp.conf**

### 2. Редактирование конфига (если нужно)

```bash
nano /opt/etc/wireguard/warp.conf

# Должны быть параметры AmneziaWG:
# (выглядят так в [Interface] секции)
Jc = 8
Jmin = 50
Jmax = 1000
S1 = 30
S2 = 32
H1 = 1811016522
H2 = 1196729875
H3 = 457766807
H4 = 1765857463
```

### 3. Применение обфускации в Keenetic

```bash
ssh admin@192.168.1.1

# Проверьте имя интерфейса WireGuard:
show interface

# Примените параметры AmneziaWG (замените Wireguard0 на ваш):
interface Wireguard0 wireguard asc 8 50 1000 30 32 1811016522 1196729875 457766807 1765857463

# Сохраните конфиг:
system configuration save
```

### 4. Включение подключения

**Веб-интерфейс:**
1. **Интернет** → **Другие подключения** → **WireGuard**
2. Переведите подключение в состояние **Включено**
3. Через 3-5 сек должна появиться зелёная точка

---

## 🛡️ Установка NFQWS

NFQWS обфускирует WireGuard handshake пакеты для обхода DPI.

### Установка

```bash
# Автоматическая через скрипт
curl -fsSL https://raw.githubusercontent.com/ldeprive3-spec/keenetic-wg-nfqws/main/install.sh | sh

# Или вручную:
ssh admin@192.168.1.1

opkg update
opkg install ca-certificates wget-ssl

# Добавление репозитория NFQWS
mkdir -p /opt/etc/opkg
echo "src/gz nfqws-keenetic https://anonym-tsk.github.io/nfqws-keenetic/all" > /opt/etc/opkg/nfqws-keenetic.conf

# Установка
opkg update
opkg install nfqws-keenetic
```

### Конфигурация NFQWS

```bash
nano /opt/etc/nfqws/nfqws.conf

# Основные параметры:
--dpi-desync=fake,split2
--dpi-desync-repeats=6
--dpi-desync-ttl=3
--dpi-desync-fooling=badsum
```

---

## 🔄 Управление сервисами

### AmneziaWG (WireGuard)

```bash
# Статус
show interface Wireguard0

# Управление через Keenetic (веб-интерфейс)
# Интернет → Другие подключения → WireGuard

# CLI команды
interface Wireguard0 enable
interface Wireguard0 disable
```

### NFQWS

```bash
# Статус
/opt/etc/init.d/S51nfqws status

# Управление
/opt/etc/init.d/S51nfqws start
/opt/etc/init.d/S51nfqws stop
/opt/etc/init.d/S51nfqws restart

# Логи
tail -f /opt/var/log/nfqws.log

# Процессы
ps | grep nfqws

# Параметры
cat /opt/etc/nfqws/nfqws.conf
```

---

## ✅ Проверка работы

### 1. Проверка AmneziaWG подключения

```bash
# На маршрутизаторе:
show interface Wireguard0

# Должны видеть:
# - Статус: Up
# - Трафик: RX/TX должны увеличиваться
# - IP: 172.16.x.x (Cloudflare WARP)
```

### 2. Проверка IP адреса

На любом устройстве в сети откройте: https://whatismyipaddress.com

**Результат должен быть:**
- ✅ IP: Cloudflare (не IP провайдера)
- ✅ Location: Зависит от вашего конфига

### 3. Проверка DNS

Откройте: https://dnsleaktest.com

**Должны видеть:**
- ✅ DNS: Cloudflare 1.1.1.1 или другие

### 4. Проверка NFQWS

```bash
# На маршрутизаторе:
ps | grep nfqws

# Должен быть процесс nfqws с параметрами обфускации
```

### 5. Проверка скорости

Откройте: https://speedtest.net

**Скорость должна быть близко к обычной интернету (без VPN).**

---

## 🐛 Решение проблем

### AmneziaWG не подключается

```bash
# Проверка параметров
show interface Wireguard0

# Если нет параметров asc:
interface Wireguard0 wireguard asc 8 50 1000 30 32 1811016522 1196729875 457766807 1765857463
system configuration save

# Перезагрузка WireGuard интерфейса
interface Wireguard0 disable
interface Wireguard0 enable
```

**Решение:**
- Проверьте конфиг warp.conf
- Убедитесь что параметры ASC введены правильно
- Попробуйте другой конфиг с warpgen.net

### NFQWS не запускается

```bash
# Проверка ошибок
/opt/etc/init.d/S51nfqws start

# Просмотр логов
tail -50 /opt/var/log/nfqws.log

# Проверка требуемых модулей
lsmod | grep nf_queue
```

**Требования:**
- ☑ IPv6 включен
- ☑ Netfilter модули установлены
- ☑ Достаточно памяти (минимум 512 MB)

**Решение:**
```bash
# Переустановка NFQWS
opkg remove nfqws-keenetic
opkg install nfqws-keenetic

# Перезагрузка маршрутизатора
reboot
```

### WireGuard отключается провайдером

**Причина:** DPI блокировка WireGuard handshake

**Решение (по порядку):**

1. **Включите NFQWS** (фрагментация пакетов):
   ```bash
   /opt/etc/init.d/S51nfqws start
   ```

2. **Измените параметры обфускации**:
   ```bash
   nano /opt/etc/nfqws/nfqws.conf
   
   # Более агрессивные параметры:
   --dpi-desync=fake,split2,disorder
   --dpi-desync-repeats=8
   --dpi-desync-ttl=5
   
   /opt/etc/init.d/S51nfqws restart
   ```

3. **Измените параметры AmneziaWG**:
   ```bash
   # Попробуйте другие значения:
   interface Wireguard0 wireguard asc 7 20 500 0 0 1811016522 1196729875 457766807 1765857463
   system configuration save
   ```

4. **Генерируйте новый конфиг** на warpgen.net (разные Endpoint адреса)

5. **Используйте AmneziaVPN вместо AmneziaWG** (если доступно)

### Медленная скорость

**Причина:** Слишком агрессивная обфускация или нагруженный маршрутизатор

**Решение:**

```bash
# Уменьшите параметры NFQWS
nano /opt/etc/nfqws/nfqws.conf

# Менее агрессивные параметры:
--dpi-desync=fake,split
--dpi-desync-repeats=2

/opt/etc/init.d/S51nfqws restart

# Проверьте нагрузку маршрутизатора
top
free -h
```

---

## 📊 Архитектура

```
Устройства в сети (ПК, телефон, TV)
           ↓
    Маршрутизатор Keenetic
           ↓
    WireGuard Interface (Wireguard0)
    - AmneziaWG Обфускация
    - Cloudflare WARP подключение
    - Параметры: Jc, Jmin, Jmax, S1, S2, H1-H4
           ↓
    NFQWS Модуль (DPI Bypass)
    - Фрагментация пакетов (split2, split3)
    - Маскировка (fake, badsum)
    - Беспорядок пакетов (disorder)
           ↓
    Провайдер DPI (видит обфускованный трафик)
    - Не видит WireGuard handshake
    - Не может заблокировать
           ↓
   Cloudflare WARP Сервер
           ↓
       Интернет (Защифровано)
```

---

## 🔧 Оптимальные параметры

### Для слабых блокировок

**AmneziaWG:**
```
Jc = 3
Jmin = 10
Jmax = 50
S1 = 0
S2 = 0
```

**NFQWS:**
```
--dpi-desync=fake,split
--dpi-desync-repeats=2
```

### Для средних блокировок

**AmneziaWG:**
```
Jc = 8
Jmin = 50
Jmax = 500
S1 = 30
S2 = 32
```

**NFQWS:**
```
--dpi-desync=fake,split2
--dpi-desync-repeats=4
--dpi-desync-ttl=3
```

### Для агрессивных блокировок

**AmneziaWG:**
```
Jc = 10
Jmin = 100
Jmax = 1200
S1 = 40
S2 = 45
```

**NFQWS:**
```
--dpi-desync=fake,split2,disorder
--dpi-desync-repeats=8
--dpi-desync-ttl=5
--dpi-desync-any-protocol
```

---

## 🔗 Полезные команды

```bash
# Быстрая диагностика
ssh admin@192.168.1.1

# Статус WireGuard
show interface | grep wireguard

# Статус NFQWS
ps | grep nfqws

# Проверка трафика
show statistics interface

# Логи NFQWS
tail -20 /opt/var/log/nfqws.log

# Проверка конфига
cat /opt/etc/nfqws/nfqws.conf | grep -v "^#"

# Статистика nfqws
nfqws --help | grep dpi-desync
```

---

## 📝 Версия

**Текущая:** 2.3  
**Последнее обновление:** 2026-01-21  
**Статус:** ✅ Активно разрабатывается

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

---

## 🤝 Вклад

Приветствуются pull requests с улучшениями!

```bash
git checkout -b feature/your-feature
git commit -am 'Add feature'
git push origin feature/your-feature
```

---

**Готово к использованию! 🚀**
