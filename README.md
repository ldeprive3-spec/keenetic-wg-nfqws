# Keenetic WireGuard + NFQWS DPI Bypass

Автоматизированная установка **NFQWS** на маршрутизаторе **Keenetic** для обфускации **WireGuard handshake пакетов** (AmneziaWG параметры) с целью обхода DPI блокировок провайдера.

🇷🇺 **Русская документация** | 🛡️ **DPI Protection** | 🚀 **Полная автоматизация**

---

## ⚡ Быстрый старт

```bash
# На маршрутизаторе Keenetic через SSH:
curl -fsSL https://raw.githubusercontent.com/ldeprive3-spec/keenetic-wg-nfqws/main/install.sh | sh
```

---

## 📋 Требования

- **Маршрутизатор:** Keenetic 5.0.4+
- **Компоненты:** IPv6, Netfilter, WireGuard VPN, Entware
- **Память:** 512 MB свободно
- **USB:** Флешка 1GB+ для Entware

---

## ⚙️ Установка компонентов (ОБЯЗАТЕЛЬНО!)

### 1. Веб-интерфейс

Откройте: **http://192.168.1.1** → **Управление** → **Параметры системы** → **Изменить набор компонентов**

### 2. Отметьте компоненты ☑

```
☑ Протокол IPv6                    (КРИТИЧНО для NFQWS)
☑ Модули ядра подсистемы Netfilter (зависит от IPv6)
☑ WireGuard VPN                    (ОБЯЗАТЕЛЬНО)
☑ Entware                          (для NFQWS)
☑ SSH сервер                       (для управления)
```

### 3. Нажмите "Обновить KeeneticOS" и дождитесь перезагрузки (5-10 минут)

### 4. Проверка установки

```bash
ssh admin@192.168.1.1

# Проверка IPv6
ip -6 addr show
# Должны видеть inet6 адреса

# Проверка Netfilter
lsmod | grep nf_queue
# Должны видеть: nf_queue, nfnetlink

# Проверка WireGuard
show interface | grep -i wireguard
# Должны видеть список WireGuard интерфейсов

# Проверка Entware
opkg --version
# Должна выдать версию opkg
```

---

## 🌐 Подключение WireGuard (Cloudflare WARP)

### 1. Создание конфига WARP

**Способ 1: AmneziaVPN + AWG 1.5 (НОВЫЙ ПРОТОКОЛ)**

1. Установите **AmneziaVPN** (основное приложение):
   - Android: https://play.google.com/store/apps/details?id=com.amnezia.vpn
   - iOS: https://apps.apple.com/app/amnezia/id6471415326
   - Windows/Linux: https://github.com/amnezia-vpn/amnezia-client/releases
   - macOS: https://github.com/amnezia-vpn/amnezia-client/releases

2. Откройте https://warpgen.net (или @WarpGenNet_bot в Telegram)

3. Выберите **AmneziaVPN** (НЕ AmneziaWG!)

4. Нажмите **Generate**

5. Скопируйте конфиг в приложение

**Способ 2: Старый WireGuard конфиг (если AWG 1.5 не нужен)**

1. Откройте https://warpgen.net

2. Выберите **WireGuard** или **AmneziaWG**

3. Нажмите **Generate** и скопируйте конфиг

### 2. Загрузка конфига в Keenetic

```bash
# Через SSH на маршрутизаторе:
# Скопируйте содержимое конфига (из шага 1)

nano /opt/etc/wireguard/warp.conf

# Вставьте конфиг, сохраните (Ctrl+X → Y → Enter)
```

**Или через веб-интерфейс:**

1. **Интернет** → **Другие подключения** → **WireGuard**
2. **Добавить подключение**
3. **Загрузить конфиг** (если есть опция)

### 3. Проверка конфига

```bash
cat /opt/etc/wireguard/warp.conf

# Должны видеть:
# [Interface]
# PrivateKey = ...
# Address = 172.16.x.x/32
# DNS = 1.1.1.1
# [Peer]
# PublicKey = ...
# AllowedIPs = 0.0.0.0/0
# Endpoint = ...
```

### 4. Включение WireGuard в Keenetic

**Веб-интерфейс:**

1. **Интернет** → **Другие подключения** → **WireGuard**
2. **Wireguard0** (или ваше имя интерфейса) → **Включить**
3. Через 3-5 сек появится зелёная точка (подключено)

**Или через CLI:**

```bash
ssh admin@192.168.1.1

# Включение
interface Wireguard0 enable

# Проверка статуса
show interface Wireguard0
# Должен быть Up, с трафиком RX/TX
```

### 5. Применение AmneziaWG параметров (ЕСЛИ ИСПОЛЬЗУЕТЕ)

**⚠️ ВАЖНО:** AmneziaWG - это **не приложение**, а **параметры WireGuard в Keenetic**!

Если конфиг с warpgen поддерживает AmneziaWG:

```bash
ssh admin@192.168.1.1

# Просмотр текущего интерфейса
show interface | grep -i wireguard

# Применение параметров (замените Wireguard0 на ваш интерфейс):
# Синтаксис: interface <name> wireguard asc <Jc> <Jmin> <Jmax> <S1> <S2> <H1> <H2> <H3> <H4>

interface Wireguard0 wireguard asc 8 50 1000 30 32 1811016522 1196729875 457766807 1765857463

# Сохранение
system configuration save

# Перезагрузка интерфейса
interface Wireguard0 disable
interface Wireguard0 enable

# Проверка
show interface Wireguard0
```

**Параметры AmneziaWG:**
- `Jc` - максимум дополнительных пакетов
- `Jmin` - минимальное время между пакетами (мс)
- `Jmax` - максимальное время между пакетами (мс)
- `S1` - размер первого фрагмента
- `S2` - размер второго фрагмента
- `H1-H4` - хеши для обфускации

---

## 🛡️ Установка NFQWS

NFQWS обфускирует **только WireGuard handshake пакеты** для обхода DPI.

### Автоматическая установка

```bash
# Используйте скрипт
curl -fsSL https://raw.githubusercontent.com/ldeprive3-spec/keenetic-wg-nfqws/main/install.sh | sh
```

### Ручная установка

```bash
ssh admin@192.168.1.1

# Обновление репозиториев
opkg update

# Установка зависимостей
opkg install ca-certificates wget-ssl

# Добавление репозитория NFQWS
mkdir -p /opt/etc/opkg
echo "src/gz nfqws-keenetic https://anonym-tsk.github.io/nfqws-keenetic/all" > /opt/etc/opkg/nfqws-keenetic.conf

# Обновление
opkg update

# Установка NFQWS
opkg install nfqws-keenetic

# Проверка
/opt/etc/init.d/S51nfqws status
```

---

## ⚙️ Конфигурация NFQWS

NFQWS автоматически создает конфиг с базовыми параметрами, но **нам нужно только для WireGuard!**

### Правильная конфигурация NFQWS

```bash
ssh admin@192.168.1.1

nano /opt/etc/nfqws/nfqws.conf
```

**Содержимое для WireGuard handshake обфускации:**

```bash
# ============================================================================
# NFQWS - WireGuard Handshake Obfuscation Only
# ============================================================================

# Целевой порт WireGuard (обычно 51820)
# Можно узнать из конфига: cat /opt/etc/wireguard/warp.conf | grep Endpoint

--dpi-desync=fake,split2
--dpi-desync-repeats=4
--dpi-desync-ttl=3
--dpi-desync-fooling=badsum

# Только для портов 51820 (WireGuard)
# ВАЖНО: Раскомментируйте если нужно ограничить только WireGuard
# --dpi-desync-udp=443,51820
```

### Запуск NFQWS

```bash
# Запуск
/opt/etc/init.d/S51nfqws start

# Проверка статуса
/opt/etc/init.d/S51nfqws status

# Логи (если нужны)
tail -20 /opt/var/log/nfqws.log

# Процесс должен быть видимым:
ps | grep nfqws
```

---

## 🔄 Управление сервисами

### WireGuard

```bash
# Статус
show interface Wireguard0

# Включение/отключение через веб-интерфейс:
# Интернет → Другие подключения → WireGuard

# Или через CLI:
interface Wireguard0 enable
interface Wireguard0 disable

# Сохранение конфига
system configuration save
```

### NFQWS

```bash
# Статус
/opt/etc/init.d/S51nfqws status

# Управление
/opt/etc/init.d/S51nfqws start
/opt/etc/init.d/S51nfqws stop
/opt/etc/init.d/S51nfqws restart

# Логи
tail -f /opt/var/log/nfqws.log

# Текущий конфиг
cat /opt/etc/nfqws/nfqws.conf
```

---

## ✅ Проверка работы

### 1. WireGuard подключение

```bash
# На маршрутизаторе:
show interface Wireguard0

# Должны видеть:
# Status: Up
# RX/TX: увеличиваются (трафик идет)
# Address: 172.16.x.x (Cloudflare WARP IP)
```

### 2. Проверка IP адреса

На устройстве в сети откройте: https://whatismyipaddress.com

**Результат:**
- ✅ IP должен быть Cloudflare (не ваш провайдер)
- ✅ Location должен совпадать с конфигом

### 3. Проверка DNS

Откройте: https://dnsleaktest.com

**Результат:**
- ✅ DNS должен быть Cloudflare 1.1.1.1 или другой из конфига

### 4. Статус NFQWS

```bash
# На маршрутизаторе:
ps | grep nfqws

# Должен быть процесс с параметрами обфускации:
# nfqws --dpi-desync=fake,split2 ...
```

### 5. Скорость интернета

Откройте: https://speedtest.net

**Результат:**
- ✅ Скорость должна быть близко к вашему обычному интернету (обфускация минимально влияет)

---

## 🐛 Решение проблем

### WireGuard не подключается

```bash
# Проверка конфига
cat /opt/etc/wireguard/warp.conf

# Проверка синтаксиса (должны быть все секции)
grep -E "^\[|PrivateKey|PublicKey|Endpoint" /opt/etc/wireguard/warp.conf

# Перезагрузка интерфейса
interface Wireguard0 disable
sleep 2
interface Wireguard0 enable

# Проверка логов (если есть)
show log | grep -i wireguard
```

**Решение:**
- Убедитесь что конфиг полный (все параметры есть)
- Попробуйте новый конфиг с warpgen.net
- Проверьте что Endpoint не пуст
- Если используете AWG 1.5, убедитесь что параметры asc применены

### NFQWS не запускается

```bash
# Проверка требуемых модулей
lsmod | grep nf_queue

# Если нет - нужны Netfilter модули в компонентах

# Проверка прав доступа
ls -la /opt/etc/init.d/S51nfqws

# Должны быть права 755 (rwxr-xr-x)
```

**Решение:**
- Установите Netfilter модули (см. раздел "Установка компонентов")
- Переустановите NFQWS: `opkg remove nfqws-keenetic && opkg install nfqws-keenetic`
- Перезагрузитесь: `reboot`

### WireGuard отключается провайдером

**Причина:** DPI обнаруживает WireGuard handshake

**Решение (по порядку):**

1. **Убедитесь что NFQWS работает:**
   ```bash
   /opt/etc/init.d/S51nfqws status
   ps | grep nfqws
   ```

2. **Измените параметры обфускации (агрессивнее):**
   ```bash
   nano /opt/etc/nfqws/nfqws.conf
   
   # Замените на:
   --dpi-desync=fake,split2,disorder
   --dpi-desync-repeats=8
   --dpi-desync-ttl=5
   
   /opt/etc/init.d/S51nfqws restart
   ```

3. **Измените параметры AmneziaWG (если используются):**
   ```bash
   interface Wireguard0 wireguard asc 10 100 1200 40 45 1811016522 1196729875 457766807 1765857463
   system configuration save
   interface Wireguard0 disable
   interface Wireguard0 enable
   ```

4. **Получите новый конфиг WARP:**
   - warpgen.net регулярно меняет endpoints
   - Создайте новый конфиг и загрузите его

5. **Если ничего не помогает:**
   - Попробуйте использовать AmneziaVPN приложение вместо WireGuard (если доступно)
   - Свяжитесь с поддержкой Keenetic

### Медленная скорость

```bash
# Проверка нагрузки маршрутизатора
top
free -h

# Если CPU/память на 100%:
# 1. Уменьшите параметры NFQWS
# 2. Перезагрузитесь
```

**Решение:**

Уменьшите параметры обфускации:

```bash
nano /opt/etc/nfqws/nfqws.conf

# Замените на более легкие:
--dpi-desync=fake,split
--dpi-desync-repeats=2
--dpi-desync-ttl=5

/opt/etc/init.d/S51nfqws restart
```

---

## 📊 Архитектура

```
Устройства в сети (ПК, телефон, TV)
           ↓
    Маршрутизатор Keenetic
           ↓
    WireGuard Interface (Wireguard0)
    - Конфиг WARP: из warpgen.net
    - Параметры: Endpoint, PrivateKey, PublicKey
    - AmneziaWG параметры (опционально): asc Jc Jmin Jmax S1 S2 H1-H4
           ↓
    NFQWS Модуль (Handshake Obfuscation)
    - Фрагментация пакетов (split2, split3)
    - Маскировка (fake, badsum)
    - Беспорядок пакетов (disorder)
           ↓
    Провайдер DPI (видит обфускованные пакеты)
    - Не видит WireGuard handshake
    - Не может заблокировать
           ↓
   Cloudflare WARP Сервер (Endpoint)
           ↓
       Интернет (Зашифрован WireGuard)
```

---

## 🔧 Оптимальные параметры

### Для слабых блокировок

**NFQWS:**
```
--dpi-desync=fake,split
--dpi-desync-repeats=2
```

**AmneziaWG (если используется):**
```
interface Wireguard0 wireguard asc 3 10 50 0 0 1811016522 1196729875 457766807 1765857463
```

### Для средних блокировок (РЕКОМЕНДУЕТСЯ)

**NFQWS:**
```
--dpi-desync=fake,split2
--dpi-desync-repeats=4
--dpi-desync-ttl=3
```

**AmneziaWG:**
```
interface Wireguard0 wireguard asc 8 50 1000 30 32 1811016522 1196729875 457766807 1765857463
```

### Для агрессивных блокировок

**NFQWS:**
```
--dpi-desync=fake,split2,disorder
--dpi-desync-repeats=8
--dpi-desync-ttl=5
```

**AmneziaWG:**
```
interface Wireguard0 wireguard asc 10 100 1200 40 45 1811016522 1196729875 457766807 1765857463
```

---

## 🔗 Полезные команды

```bash
# Быстрая диагностика
ssh admin@192.168.1.1

# Статус всех интерфейсов
show interface

# Статус только WireGuard
show interface | grep -i wireguard

# Статус NFQWS
/opt/etc/init.d/S51nfqws status

# Процесс NFQWS
ps | grep nfqws

# Просмотр конфига WARP
cat /opt/etc/wireguard/warp.conf

# Просмотр конфига NFQWS
cat /opt/etc/nfqws/nfqws.conf

# Логи NFQWS
tail -20 /opt/var/log/nfqws.log

# Перезагрузка WireGuard
interface Wireguard0 disable && interface Wireguard0 enable

# Перезагрузка NFQWS
/opt/etc/init.d/S51nfqws restart

# Сохранение конфига
system configuration save

# Полная перезагрузка маршрутизатора
reboot
```

---

## 📝 Версия

**Текущая:** 2.3  
**Последнее обновление:** 2026-01-21  
**Статус:** ✅ Активно разрабатывается

---

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

---

## 🤝 Вклад

Приветствуются pull requests с улучшениями!

```bash
git checkout -b feature/your-feature
git commit -am 'Add feature'
git push origin feature/your-feature
```

---

**Готово к использованию! 🚀**
